---
phase: 04-monorepo-scaffold-infrastructure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - eslint.config.js
  - .prettierrc
  - commitlint.config.js
  - vitest.workspace.ts
  - packages/types/package.json
  - packages/types/tsconfig.json
  - packages/types/src/index.ts
  - packages/config/package.json
  - packages/config/tsconfig.json
  - packages/config/src/index.ts
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "pnpm install runs at the repo root and symlinks all workspace packages"
    - "pnpm -r tsc --noEmit passes with zero errors across all packages"
    - "A service package.json can declare @affiliate/types as workspace:* and import from it without path errors"
    - "ESLint flat config is active and enforces rules across all packages via pnpm -r eslint ."
    - "Prettier formatting config is shared from the root"
    - "commitlint.config.js enforces Conventional Commits format"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "workspace glob — apps/* and packages/*"
      contains: "apps/*"
    - path: "tsconfig.base.json"
      provides: "shared TypeScript compilerOptions"
      contains: "NodeNext"
    - path: "eslint.config.js"
      provides: "ESLint 9 flat config"
    - path: "packages/types/src/index.ts"
      provides: "placeholder export so tsc --noEmit doesn't fail on empty package"
      contains: "export {}"
    - path: "packages/config/src/index.ts"
      provides: "placeholder export"
      contains: "export {}"
  key_links:
    - from: "packages/types/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends"
      pattern: "extends.*tsconfig.base.json"
    - from: "packages/config/package.json"
      to: "@affiliate/types"
      via: "workspace:* dependency"
      pattern: "workspace:\\*"
---

<objective>
Scaffold the pnpm monorepo root — workspace config, shared TypeScript/ESLint/Prettier/commitlint configs, and stub packages for `packages/types` and `packages/config`.

Purpose: This is the foundation every other package and service depends on. Nothing can import `@affiliate/types` or run `pnpm -r tsc --noEmit` until this plan is complete.
Output: A working pnpm workspace where cross-package TypeScript imports resolve and all tooling configs are in place.
</objective>

<execution_context>
@/Users/davids/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davids/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-monorepo-scaffold-infrastructure/04-CONTEXT.md
@.planning/phases/04-monorepo-scaffold-infrastructure/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pnpm workspace root config and shared tooling configs</name>
  <files>
    pnpm-workspace.yaml
    package.json
    tsconfig.base.json
    eslint.config.js
    .prettierrc
    commitlint.config.js
    vitest.workspace.ts
  </files>
  <action>
Create the following files at the repo root (alongside the existing monolith files — do NOT move or touch server.js, routes/, services/, etc.):

**pnpm-workspace.yaml:**
```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

**package.json** — replace existing root package.json scripts section (preserve all existing fields, only add/update `scripts` and add devDependencies for workspace tooling). Merge carefully:
- Add scripts: `"typecheck": "pnpm -r tsc --noEmit"`, `"test": "pnpm -r vitest run"`, `"lint": "pnpm -r eslint ."`, `"dev": "docker compose -f docker-compose.yml -f docker-compose.override.yml up"`
- Add to devDependencies: `"typescript": "^5.0.0"`, `"vitest": "4.1.5"`, `"eslint": "^9.0.0"`, `"prettier": "^3.0.0"`, `"@commitlint/cli": "latest"`, `"@commitlint/config-conventional": "latest"`
- Set `"private": true` at root level

**tsconfig.base.json:**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "incremental": true,
    "skipLibCheck": true
  }
}
```

**eslint.config.js** (ESLint 9 flat config — not .eslintrc):
```js
import js from '@eslint/js';

export default [
  js.configs.recommended,
  {
    rules: {
      'no-unused-vars': 'warn',
      'no-console': 'off',
    },
    ignores: ['**/dist/**', '**/node_modules/**'],
  },
];
```
Note: `@eslint/js` must be in root devDependencies.

**.prettierrc:**
```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "es5",
  "printWidth": 100
}
```

**commitlint.config.js:**
```js
export default {
  extends: ['@commitlint/config-conventional'],
};
```

**vitest.workspace.ts:**
```typescript
import { defineWorkspace } from 'vitest/config';

export default defineWorkspace([
  'packages/*/vitest.config.ts',
  'apps/*/vitest.config.ts',
]);
```

After creating files, run `pnpm install` at the root to generate/update pnpm-lock.yaml.
  </action>
  <verify>
    <automated>cd /Users/davids/Development/Learning/affiliate-heaven && ls pnpm-workspace.yaml tsconfig.base.json eslint.config.js .prettierrc commitlint.config.js vitest.workspace.ts</automated>
    <manual>Confirm all six files exist at repo root</manual>
  </verify>
  <done>All root config files exist; pnpm install completes without errors</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold packages/types and packages/config with workspace:* cross-package import</name>
  <files>
    packages/types/package.json
    packages/types/tsconfig.json
    packages/types/src/index.ts
    packages/types/vitest.config.ts
    packages/config/package.json
    packages/config/tsconfig.json
    packages/config/src/index.ts
    packages/config/vitest.config.ts
  </files>
  <action>
Create directory structure and files for both shared packages.

**packages/types/package.json:**
```json
{
  "name": "@affiliate/types",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit"
  }
}
```

**packages/types/tsconfig.json:**
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}
```

**packages/types/src/index.ts** (placeholder — prevents `tsc --noEmit` failure on empty package):
```typescript
// Shared TypeScript types for Affiliate Heaven microservices
// Populated as services are extracted in Phases 5–10

export {};
```

**packages/types/vitest.config.ts:**
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
  },
});
```

**packages/config/package.json:**
```json
{
  "name": "@affiliate/config",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@affiliate/types": "workspace:*"
  }
}
```

**packages/config/tsconfig.json:**
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "outDir": "dist",
    "rootDir": "src"
  },
  "references": [
    { "path": "../types" }
  ],
  "include": ["src"]
}
```

**packages/config/src/index.ts** (placeholder):
```typescript
// Shared env-var parsing and zod schemas for Affiliate Heaven microservices
// Populated as services are extracted in Phases 5–10

export {};
```

**packages/config/vitest.config.ts:**
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
  },
});
```

After creating files, run `pnpm install` again so workspace symlinks are updated for the new packages.
  </action>
  <verify>
    <automated>cd /Users/davids/Development/Learning/affiliate-heaven && pnpm -r tsc --noEmit 2>&1 | tail -5</automated>
    <manual>Output should show no TypeScript errors. It may warn about missing packages that will be added in Plan 02/03.</manual>
    <sampling_rate>run after this task, before next plan begins</sampling_rate>
  </verify>
  <done>`pnpm -r tsc --noEmit` passes with zero errors across packages/types and packages/config; packages are symlinked in node_modules/.pnpm</done>
</task>

</tasks>

<verification>
- `pnpm-workspace.yaml` declares `apps/*` and `packages/*`
- `tsconfig.base.json` has `"module": "NodeNext"` and `"moduleResolution": "NodeNext"`
- `eslint.config.js` uses ESLint 9 flat config format (not .eslintrc)
- Both `packages/types/src/index.ts` and `packages/config/src/index.ts` contain `export {}` placeholder
- `packages/config/package.json` depends on `@affiliate/types` via `workspace:*`
- `pnpm -r tsc --noEmit` exits with code 0
</verification>

<success_criteria>
- pnpm workspace is initialized with correct globs
- All shared tooling configs (TypeScript, ESLint, Prettier, commitlint, Vitest) exist at root
- packages/types and packages/config have correct tsconfig project references and placeholder exports
- `pnpm -r tsc --noEmit` passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-monorepo-scaffold-infrastructure/04-01-SUMMARY.md`
</output>
