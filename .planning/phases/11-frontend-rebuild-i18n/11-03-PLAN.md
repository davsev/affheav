---
phase: 11-frontend-rebuild-i18n
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/locales/he.json
  - apps/web/src/locales/en.json
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03
  - I18N-04

must_haves:
  truths:
    - "he.json contains all keys from public/i18n/strings.js he: block — no missing keys"
    - "en.json contains all keys from public/i18n/strings.js en: block — no missing keys"
    - "Every key in he.json has a corresponding key in en.json (same set, different values)"
    - "No hardcoded Hebrew strings remain in any apps/web component (enforced by convention — no component files exist yet, so this is a prerequisite gate)"
  artifacts:
    - path: "apps/web/src/locales/he.json"
      provides: "Complete Hebrew translation map for all UI strings"
      contains: "loginWithGoogle"
    - path: "apps/web/src/locales/en.json"
      provides: "Complete English translation map for all UI strings"
      contains: "loginWithGoogle"
  key_links:
    - from: "apps/web/src/lib/i18n.ts"
      to: "apps/web/src/locales/he.json"
      via: "static import"
      pattern: "from.*locales/he"
    - from: "apps/web/src/lib/i18n.ts"
      to: "apps/web/src/locales/en.json"
      via: "static import"
      pattern: "from.*locales/en"
---

<objective>
Produce the complete `he.json` and `en.json` locale files by migrating all strings from `public/i18n/strings.js`. These files are the single source of truth for all user-visible text in the new frontend.

Purpose: Every component in Plans 11-04 through 11-08 must use `t('key')` — the locale files must exist and be complete before any component is written.
Output: Two JSON files with identical key sets: `apps/web/src/locales/he.json` (Hebrew) and `apps/web/src/locales/en.json` (English).
</objective>

<execution_context>
@/Users/davids/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davids/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-frontend-rebuild-i18n/11-RESEARCH.md
@public/i18n/strings.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract he.json and en.json from public/i18n/strings.js</name>
  <files>
    apps/web/src/locales/he.json
    apps/web/src/locales/en.json
  </files>
  <action>
Read `public/i18n/strings.js` in full. It exports a `STRINGS` object with `he:` and `en:` keys.

Extract `STRINGS.he` as `apps/web/src/locales/he.json` — a flat JSON object. Every key in the `he:` block becomes a top-level key.

Extract `STRINGS.en` as `apps/web/src/locales/en.json`. Where a key exists in `en:` use its value; where a key is missing from `en:` (English block is often a subset), use the Hebrew value as a fallback placeholder (this is acceptable — react-i18next will fall back to `fallbackLng: 'he'` anyway).

Additionally, add keys needed for the new FE-05 FeatureFlags screen and FE-04 Credentials screen that do NOT exist in the current strings.js:

**New keys to add to BOTH locale files:**

```json
{
  "navFeatureFlags": "דגלי פיצ'רים",
  "navCredentials": "חיבורי חשבון",
  "featureFlagsTitle": "דגלי פיצ'רים",
  "featureFlagEnabled": "פעיל",
  "featureFlagDisabled": "כבוי",
  "credentialsTitle": "חיבורי חשבון",
  "credentialConnected": "מחובר",
  "credentialDisconnected": "לא מחובר",
  "credentialConnect": "חבר",
  "credentialDisconnect": "נתק",
  "keepExisting": "••••••••••",
  "saveChanges": "שמור שינויים",
  "cancel": "ביטול",
  "confirm": "אישור",
  "delete": "מחק",
  "edit": "עריכה",
  "add": "הוסף",
  "refresh": "רענן",
  "loading": "טוען...",
  "error": "שגיאה",
  "success": "הצלחה"
}
```

English values for new keys in en.json:
```json
{
  "navFeatureFlags": "Feature Flags",
  "navCredentials": "Account Connections",
  "featureFlagsTitle": "Feature Flags",
  "featureFlagEnabled": "Enabled",
  "featureFlagDisabled": "Disabled",
  "credentialsTitle": "Account Connections",
  "credentialConnected": "Connected",
  "credentialDisconnected": "Not Connected",
  "credentialConnect": "Connect",
  "credentialDisconnect": "Disconnect",
  "keepExisting": "Keep existing",
  "saveChanges": "Save Changes",
  "cancel": "Cancel",
  "confirm": "Confirm",
  "delete": "Delete",
  "edit": "Edit",
  "add": "Add",
  "refresh": "Refresh",
  "loading": "Loading...",
  "error": "Error",
  "success": "Success"
}
```

Write both files as properly formatted JSON (2-space indentation).
  </action>
  <verify>
    <automated>node -e "const he = require('./apps/web/src/locales/he.json'); const en = require('./apps/web/src/locales/en.json'); const heKeys = Object.keys(he).sort(); const enKeys = Object.keys(en).sort(); const missing = heKeys.filter(k => !enKeys.includes(k)); if(missing.length) { console.error('Keys in he.json missing from en.json:', missing); process.exit(1); } console.log('OK: he.json has', heKeys.length, 'keys, en.json has', enKeys.length, 'keys, sets match');" 2>&1</automated>
  </verify>
  <done>he.json and en.json exist with identical key sets. he.json has ≥100 keys (covering all strings from public/i18n/strings.js). No key present in he.json is absent from en.json.</done>
</task>

</tasks>

<verification>
- `apps/web/src/locales/he.json` is valid JSON with all keys from public/i18n/strings.js he: block
- `apps/web/src/locales/en.json` is valid JSON with identical key set
- Both files include the new FE-04 and FE-05 keys (navFeatureFlags, credentialsTitle, etc.)
- Key count check passes (node validation script above exits 0)
</verification>

<success_criteria>
Both locale files are complete, valid JSON with matching key sets. Any component author in Plans 11-04 through 11-08 can use any existing UI string via `t('key')` without modifying these files.
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-rebuild-i18n/11-03-SUMMARY.md`
</output>
