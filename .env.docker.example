# Docker Compose local dev environment
# Copy to .env.docker and fill in strong random passwords.
# Generate one with: openssl rand -base64 32
#
# NEVER commit .env.docker — it is in .gitignore

POSTGRES_PASSWORD=change_me

MONOLITH_DB_PASSWORD=change_me
AUTH_DB_PASSWORD=change_me
USERS_DB_PASSWORD=change_me
SUBJECTS_DB_PASSWORD=change_me
PRODUCTS_DB_PASSWORD=change_me
AI_WRITER_DB_PASSWORD=change_me
CHANNELS_DB_PASSWORD=change_me
SCHEDULER_DB_PASSWORD=change_me
BROADCASTER_DB_PASSWORD=change_me
GATEWAY_DB_PASSWORD=change_me
